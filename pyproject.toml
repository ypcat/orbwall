[project]
name = "orbwall"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["rumps>=0.4.0"]

[project.scripts]
orbwall = "orbwall:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
